{
  "variables": {
    "key_name": "",
    "key_path": "",
    "aws_access_key": "",
    "aws_secret_key": "",
    "aws_region": "eu-west-1",
    "aws_build_regions" : "",
    "aws_instance_type": "t2.small",
    "aws_vpc_id": "",
    "aws_subnet_id": "",
    "ami_name_prefix": "kubernetes",
    "bastion_host": "",
    "bastion_username": "ec2-user",
    "kubernetes_version": "",
    "base_version": "",
    "stream_tag": ""
  },
  "builders": [{
    "type": "amazon-ebs",
    "access_key": "{{user `aws_access_key`}}",
    "secret_key": "{{user `aws_secret_key`}}",
    "region": "{{user `aws_region`}}",
    "source_ami_filter": {
      "filters": {
        "virtualization-type": "hvm",
        "name": "base-{{user `base_version`}}-*",
        "root-device-type": "ebs"
      },
      "owners": ["991512327633"],
      "most_recent": true
    },
    "instance_type": "{{user `aws_instance_type`}}",
    "vpc_id": "{{user `aws_vpc_id`}}",
    "subnet_id": "{{user `aws_subnet_id`}}",
    "ami_name": "{{user `ami_name_prefix`}}-{{user `kubernetes_version`}}-{{user `base_version`}}-{{timestamp}}",
    "ami_regions": "{{user `aws_build_regions`}}",
    "communicator": "ssh",
    "ssh_pty": "true",
    "ssh_timeout": "10m",
    "ssh_username": "ubuntu",
    "ssh_keypair_name": "{{user `key_name`}}",
    "ssh_private_key_file": "{{user `key_path`}}",
    "ssh_bastion_host": "{{user `bastion_host`}}",
    "ssh_bastion_username": "{{user `bastion_username`}}",
    "ssh_bastion_private_key_file": "{{user `key_path`}}",
    "tags": {
      "stream": "{{user `stream_tag`}}"
    }
  }],
  "provisioners": [
    {
      "type": "shell",
      "script": "scripts/kubernetes.sh",
      "environment_vars": [
        "KUBERNETES_VERSION={{user `kubernetes_version`}}"
      ],
      "execute_command": "chmod +x {{ .Path }}; {{ .Vars }} sudo -E '{{ .Path }}'"
    }
  ]
}
