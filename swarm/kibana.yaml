  logging.verbose: false
  logging.quiet: true
  kibana.index: ".kibana"
  kibana.defaultAppId: "discover"
  server.port: 5601
  server.host: "0"
  server.ssl.enabled: true
  server.ssl.key: "/usr/share/kibana/config/kibana_key.pem"
  server.ssl.certificate: "/usr/share/kibana/config/kibana_cert.pem"
  server.ssl.certificateAuthorities: ["/usr/share/kibana/config/ca_cert.pem"]
  elasticsearch.username: "kibana"
  elasticsearch.password: "changeme"
  elasticsearch.ssl.verificationMode: "certificate"
  elasticsearch.ssl.key: "/usr/share/kibana/config/kibana_key.pem"
  elasticsearch.ssl.certificate: "/usr/share/kibana/config/kibana_cert.pem"
  elasticsearch.ssl.certificateAuthorities: ["/usr/share/kibana/config/ca_cert.pem"]
  xpack.reporting.encryptionKey: "cafebabe"
  xpack.monitoring.elasticsearch.username: "kibana"
  xpack.monitoring.elasticsearch.password: "changeme"
  xpack.monitoring.elasticsearch.ssl.verificationMode: "certificate"
  xpack.monitoring.elasticsearch.ssl.certificateAuthorities: ["/usr/share/kibana/config/ca_cert.pem"]
